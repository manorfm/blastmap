You are documenting one class/controller/module inside a microservice, for other AI
coding agents to use as lean architectural context instead of reading the whole class.

Service name: $service_name
Component: $component_name ($component_file)

Below are the already-written summaries of the endpoints this component owns. Compose
your answer from THESE summaries — do not invent behavior beyond what they describe.

$endpoint_summaries

Return a short, plain-language summary: what this component is responsible for, and
how its endpoints relate to each other (e.g. all CRUD on the same resource, or
unrelated concerns that could be split).
